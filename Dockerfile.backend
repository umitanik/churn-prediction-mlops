FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ /app/src/

ENV MODEL_PATH=/app/models/catboost_churn_model.pkl

CMD ["uvicorn", "src.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
